export const environment = {
  production: true,
  apiUrl: 'https://ibirqld8do.c35.airoapp.ai/api',
  // Firebase google-services.json is not in the repo; registering FCM crashes the APK.
  pushNotifications: false,
};
