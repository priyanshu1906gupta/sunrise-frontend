export const environment = {
  production: true,
  // Replace YOUR_DOMAIN with the live GoDaddy host before `npm run publish:native`.
  // You can also set NATIVE_API_URL or PUBLIC_URL (e.g. https://yourdomain.com).
  apiUrl: 'https://YOUR_DOMAIN.com/api',
};
